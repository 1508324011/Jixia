# Trellis Tasks

Each task directory contains:

- `task.json`: task metadata.
- `prd.md`: requirements and acceptance criteria.
- `context/`: JSONL context files for implement/check/debug phases.

Task directories use the format `MM-DD-slug`.
