# Code Reuse Guide

Before adding new helpers:

1. Search for an existing equivalent.
2. Reuse existing abstractions when they match the domain.
3. Keep one-off logic inline unless it names a real domain concept.
4. Avoid speculative framework code before the MVP needs it.
