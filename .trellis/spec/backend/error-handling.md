# Backend Error Handling

To be filled by the team.

Initial MVP constraints:

- Permission failures are decided by the API.
- Document save conflicts must return a conflict payload instead of overwriting.
- Upload confirmation failures use the locked `failureReason` enum.
